// Функция принимает два натуральных числа x и n и возвращает x в степени n
const exponentiation = (x, n) => Math.pow(x, n);

// Получаем результат выполнения функции
let result = exponentiation(5, 2);

// Выводим результат в консоль
console.log(result);